<div class="container mt-4">
  <div class="card">
    <div class="card-header">
      <h2>商品編集</h2>
    </div>
    <div class="card-body">
      <%= render 'form', product: @product %>
    </div>
    <div class="card-footer">
      <%= link_to '詳細', @product, class: 'btn btn-info' %>
      <%= link_to '戻る', products_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div> 