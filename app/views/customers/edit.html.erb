<div class="container mt-4">
  <div class="card">
    <div class="card-header">
      <h2>顧客編集</h2>
    </div>
    <div class="card-body">
      <%= render 'form', customer: @customer %>
    </div>
    <div class="card-footer">
      <%= link_to '詳細', @customer, class: 'btn btn-info' %>
      <%= link_to '戻る', customers_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div> 