<div class="container mt-4">
  <div class="card">
    <div class="card-header">
      <h2>税率編集</h2>
    </div>
    <div class="card-body">
      <%= render 'form', tax_rate: @tax_rate %>
    </div>
    <div class="card-footer">
      <%= link_to '詳細', @tax_rate, class: 'btn btn-info' %>
      <%= link_to '戻る', tax_rates_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div> 