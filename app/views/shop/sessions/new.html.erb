<h1>会員ログイン</h1>

<div class="mb-4">
  <p>会員ログインIDとパスワードを入力してください。</p>
  <p>パスワードが発行されていない場合は、担当者にお問い合わせください。</p>
</div>

<%= form_with(model: @customer, url: shop_login_path, local: true) do |f| %>
  <div class="form-group">
    <%= f.label :customer_code, 'ログインID' %>
    <%= f.text_field :customer_code, class: 'form-control' %>
  </div>
  
  <div class="form-group">
    <%= f.label :password, 'パスワード' %>
    <%= f.password_field :password, class: 'form-control' %>
  </div>
  
  <div class="form-actions mt-3">
    <%= f.submit 'ログイン', class: 'btn btn-primary' %>
    <%= link_to 'カートに戻る', shop_cart_path, class: 'btn btn-outline-secondary' %>
  </div>
<% end %> 